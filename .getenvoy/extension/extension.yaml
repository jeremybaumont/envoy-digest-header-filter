#
# Envoy Wasm extension created with getenvoy toolkit.
#
kind: Extension

name: me.filters.http.envoy_digest_header_filter

category: envoy.filters.http
language: rust

# Runtime the extension is being developed against.
runtime:
  envoy:
    version: wasm:1.15
